spring:
  application:
    name: messages
  cloud:
    config:
      url: http://localhost:8888
server:
  port: 8183

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
